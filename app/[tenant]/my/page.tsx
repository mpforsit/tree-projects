import { strings } from "@/lib/strings";

/** Placeholder — "Meine Arbeit" (alarms + cross-tree list) lands in M7. */
export default function MyWorkPage() {
  return <h1 style={{ fontSize: 22 }}>{strings.shell.myWork}</h1>;
}
